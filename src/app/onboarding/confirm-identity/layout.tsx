import type { Metadata } from "next";
import type { ReactNode } from "react";

export const metadata: Metadata = { title: "Confirm Your Identity | yoRento" };

export default function ConfirmIdentityLayout({ children }: { children: ReactNode }) {
  return children;
}

import type { Metadata } from "next";
import type { ReactNode } from "react";

export const metadata: Metadata = { title: "Verify Your Identity | yoRento" };

export default function VerifyIdLayout({ children }: { children: ReactNode }) {
  return children;
}

import type { Metadata } from "next";
import type { ReactNode } from "react";

export const metadata: Metadata = { title: "Verify Your Phone | yoRento" };

export default function VerifyPhoneLayout({ children }: { children: ReactNode }) {
  return children;
}
